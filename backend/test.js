const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const count = await prisma.earthquake.count(); const recent = await prisma.earthquake.findMany({ orderBy: { time: 'desc' }, take: 5 }); console.log('Total:', count); console.log('Recent:', recent.map(e => ({ id: e.id, time: e.time, mag: e.magnitude }))); } main().finally(() => prisma.$disconnect());
